package controlador.gestor;

import android.widget.EditText;
import android.widget.ImageButton;

/**
 * Interface Listener dels botons de la barra del comunicador
 * @author Jordi Gómez Lozano
 */
public interface OnFragmentInteractionUserListener {
    void onToolbarButtonPressed(ImageButton button);
}
